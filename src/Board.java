
public class Board {
	// read all squares int 2d array including types doors ?(first letter of
	// room name)D(door)S(direction south east...)
	// connect all squares that are not walls
	// connect stairs
	public void draw() {
		//loop through all squares calling draw method 
	} 
}
